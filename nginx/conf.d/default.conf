server {
  listen 80;
  server_name _;

  # Streamlit app running in container "app" on 8501
  location / {
    proxy_pass http://app:8501/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
